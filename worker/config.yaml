version: 1
formatters:
  simple:
    class: logging.Formatter
    format: '%(asctime)s - %(message)s'
handlers:
  console:
    class: logging.StreamHandler
    formatter: simple
    stream: ext://sys.stdout
loggers:
  mano_client:
    level: DEBUG
    handlers: [console]
